---
const { schoolName, degree, startDate, endDate } = Astro.props;
---

<div class="school">
  <h3>{schoolName}</h3>
  <h4>{degree}</h4>
  <span>{startDate}</span>
  <span>-</span>
  <span>{endDate}</span>
  <slot />
</div>
